import NumberedFrames from "@/components/NumberedFrames";

export default function NumberedFramesSection() {
  return (
    <div className="w-full h-full flex items-center justify-center bg-[#f5f5f5]">
      <NumberedFrames />
    </div>
  );
}
